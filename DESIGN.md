# cdp-browser-control 顶层设计

> 这是本 skill 的**设计本体**，活文档。一切实现、改动、重构都从本文档出发，并在本文档上演进。由开发者（人）高度掌握，Agent 对本文档改动后的提交，必需由人单独审核，即便是 Agent 全权负责的任务也是如此。

---

## 根问题

Agent 的上下文固定且极小；浏览器页面是巨大、持续变化、嵌套隐藏、元素匿名的**活状态**。本 skill 的全部职责是**在两者之间架桥**——让 agent 用最少的 token、最可靠的句柄，去**看清 → 指到 → 动手 → 得到反馈**。

一切抽象从这条桥长出来；桥的形态可以整个重搭。三枝（感知 / 指认 / 操作）是 agent 的三类意图，从根问题自然推出，但不是教条——若有更优的根结构，枝亦可动摇。

设计理念长成一棵树，新的抽象设计都是在树枝上分叉出新枝，一旦某个方向钻牛角尖堆屎山了，就把从根到叶的路径，从前向后审视，看每个抽象节点是否必要，是否是走了错误路线，是否长出了错误分支优化本不应该存在的问题。

许多人在读历史会产生一种错觉，觉得每个王朝的末期是一群坏人在干坏事才导致国家衰弱的。其实不是，大部分时候一个帝国的衰亡，是一群在这个系统里完全理性的好人，按照一套已经僵死的好规矩，合力把这艘船开进了冰山。亡国之君往往不是死于无法无天，而是死于作茧自缚。那些曾经维持了帝国稳定的法律和惯例，经过两百年的堆积，变成了一张密不透风的网。每一个既得利益者都趴在这张网上，合法地、合规地、甚至合乎道德地吸食帝国的血液。

Agent 的开发也是这样，一步加一个约束，然后相信前面的约束，继续加新的约束，不可避免的走向死亡螺旋。就成了屎山。必须每发展一定的程度，就重新做顶层抽象设计，进行激进的改革。

---

## 设计律（先于一切实现）

1. **从根到叶每个节点经得起"为什么存在"的拷问。** 一个抽象若只为修补另一个抽象的副作用而生——砍。

2. **一个问题一套机制。** 同一问题冒出第二个补丁时，停下从根到叶重审路径，而非叠第三个。

3. **设计规定"传达什么"，不规定"长什么样"。** 一切输出形态（标注符号、文件列数、命令语法糖、占位字形）都是可迭代的实现细节；本设计只锁定每个输出要承载的信息，不锁其字面形态。日后可整套换皮。

4. **零兼容。** 旧名 / 旧格式 / 旧语义一律删，不写迁移、不留 fallback。


## 顶层设计

- 浏览器地基
  - 浏览器连接
    - 探测用户可用 / 默认浏览器，独立用户数据目录拉起 CDP 端口
    - 一切命令自愈式确保连接（未起自动启动、就绪零开销），agent 不管理浏览器生命周期
  - 页面世代
    - 一个 document = 一个世代。一切会话状态**全部挂在世代上，世代亡则全亡**
    - 整页导航 / 刷新 = 切世代；
    - 锚点跳转 / pushState = URL 变，而 document 未换，**不切世代**
    - 世代切换由**工具察觉并主动告知**（反馈自动升格为新页整页感知），agent 永不自查、永不猜
  - 页面选择
    - --target 指定页面
    - list 第一条即激活页
    - 激活页为默认作用目标，无需 --target 指定
- 浏览器任务
  - 感知
    - view 压缩页面
      - 站点有 recipe，压缩为指定格式，见 聚焦/被动聚焦/recipe
      - 站点无 recipe，fallback 压缩为紧凑树
        - 全部内容文本、图片替代文本、可交互元素
        - 结构从属关系可辨
        - 用标识代表元素状态
          - 视口内外有标
          - 折叠区域一行占位
          - 用 ref 指向 DOM，感知即指认的入口，可直接操作 click fill hover focus
        - shadow 子树穿透
        - 承诺"完整页面"：懒加载自动触发、渲染安定之后才建树
        - 压缩形态自由：丢垃圾标签、折叠包装、内联合并、截断，皆可随意迭代——只要上述承诺不破
        - 已知盲区
          - iframe 子文档，至少要在树上标注此处有 iframe
          - 遮挡 / 弹窗，modal 压页时，被盖区域应有标注，弹窗块应可辨
    - 聚焦
      - 主动聚焦
        - view ref 局部感知指定区域，ancestor 抬升到祖链的语义容器
        - view --visible-only，只看当前屏，仅用于确认眼前
        - article ref 将富文本区域转为 Markdown 文章
      - 被动聚焦 / 规则化聚焦
        - 规则保存于 `rules/` ，build 后更新至 `dist/rules/` 
        - 规则库以 agent 编辑维护为主，等待更好的管理方案
        - 跳转链接(link.zhihu.com/?target=…)自动解回真实 URL
        - fold 规则，用于感知树折叠低信息区
          - 通配符站点、selector 匹配
          - 折叠不销毁，以该 ref 局部感知即展开
        - ignore-links 规则，article 去除垃圾 URL
        - recipe 站点规则，常用页面 view 全页时只筛选重点信息，以期望的格式结构化输出
          - 多文件，每文件可存多份规则，应对同一站点的不同路径，保存于 `rules/` 
          - URL 作用域(hostname+pathname glob)匹配，`view`/`fetch` 默认启用、未命中回落树
          - 过程式规则，recipe 是 Node 模块，复用已有 API，写"站点语义"不重造引擎感知器
          - 引擎原语接管可复用/易错/有状态部分：只读探针(refOf 只查不注册)、展开再读(cdp.read，article 保持纯读)、呈现(_lib.entry/abridge)
  - 指认
    - ref：会话句柄——树上直接点名
      - 可通过 ref 直接操作 DOM
      - 同世代内稳定：单调只增，永不清零，除非换世代
      - 父链记全
        - ancestor 参数爬父，把内容叶子抬到语义容器
        - info 摊开从根到目标的祖先链、每层给锚点候选，便于 agent 写 selector 供脚本沉淀
      - 失效处理
        - 同世代，沿父链找最近存活容器，就地局部感知
        - 换世代，说明为新页面，且附新页整页感知
    - selector：持久句柄——跨页面、跨会话、进规则库
      - shadow 内元素给 `>>>` 穿透链
  - 操作
    - 动作
      - click / fill / focus / hover / press-key / scroll；目标一律 ref
      - 动作前自动将目标自动滚入视口
    - 反馈
      - 回报：新增内容（增量 ref，即拿即用）/ 文本变化（旧 → 新）/ 新开与关闭 tab / URL 跳转
      - 世代切换 → 自动升格为新页整页感知，言明旧 ref 已死
      - 降噪：播放器子树、连跳时间戳等机械变化折叠；点赞数等真信号保留
      - 静默窗口：动作后留渲染时间再采（时长可调）
      - 对话框：alert / confirm 弹出即感知、可处置，不许卡死整条管线
      - 结果诚实：每个动作要么生效，要么说清为何没生效（未命中 / 被遮挡 / 世代切换），永不无声
- 配套
  - 脚本化：run 一次执行多步（全局 cdp API，省模型往返）；跑通的探索**沉淀为 sites/ 站点原语**（自包含、带验证生命周期）
  - fetch：open → 就绪 → 整页感知 → close 一条龙，替代 web fetch
  - 日志
    - 新开 Tab 注入 Page.addScriptToEvaluateOnNewDocument，刷新自动补装
    - 用户打开新 Tab，用 daemon 常驻注入
  - eval：逃生舱，不是日常路，日常定位一律 view / ref / selector

  
## 命令行

- 浏览器连接
  - `list` 列出所有 page tab,同时确保 CDP 浏览器已就绪
  - `open` 新开一个 tab
  - `close` 关闭一个 tab
  - `kill` 强制结束 9222 端口上的浏览器进程并等端口释放
  - `activate` 把指定 tab 拉到前台
- 脚本执行
  - `run` 跑一段自动化脚本(脚本内用全局 cdp API)
- 页面导航
  - `navigate` 让当前 tab 导航到指定 url
- 感知
  - `view` 感知页面：命中站点 recipe 输出聚焦摘要，否则压成带结构、带信息的紧凑树；`--tree`/建树意图强制树
  - `fetch <url>` 临时打开页面，用 `view`/`recipe` 感知抓取内容后，关闭页面，替代 web fetch MCP
- 自由执行
  - `eval` 在页面内执行任意 JS,回传 JSON 值
- 指认
  - `info <n> [--ancestor <k>]` 给目标元素的父链(tag/id/class/语义属性逐层),并附该 DOM 的 selector(已实现,接 `api.info`)
- 操作（目标均为 ref）
  - `click` 点击元素
  - `fill` 往输入框填值并触发 input/change
  - `focus` 聚焦元素
  - `hover` 鼠标悬停在元素上
  - `press-key` 按键 / 组合键(如 Enter、Ctrl+Shift+A)
  - `get-focus` 查看当前焦点落在哪个元素
- 观察
  - `screenshot` 截图
  - `logs` 读页面控制台日志

## 临时方案

fold / ignore-links 规则手动编辑 `rules/*.csv`(seed 自 `rules/`);recipe 手写 `rules/recipes/<site>.js`。

## TODO

- 引入 Playwright ，复用已成熟巨人的 API，供脚本编排用
- 改进目录结构，skill 相关放入项目子文件夹，不与项目开发造成干扰
- 想一个新方法，免去每次都 `node ~/.claude/skills/cdp-browser-control/dist/cdp.js` 这么长的路径前缀
  - skill 改名，更好听好记的名
  - 上传到 npm
  - 安装为系统可执行的 cli
- 反馈格式改进
  - list 不用列出全长 id
- search 功能，子代理调用，多个搜索引擎，配 recipe ，多引擎搜索摘要聚合，agent 筛选后并行 fetch，子代理返回给出分析结果
# cdp-browser-control 顶层设计

> 这是本 skill 的**设计本体**，活文档。一切实现、改动、重构都从本文档出发，并在本文档上演进。
> 当前状态：根律与「感知」已细化；「指认 / 操作 / 聚焦」已按既定律立到意图层，**仍待逐节细化**（标 ⏳）。

---

## 根问题

Agent 的上下文固定且极小；浏览器页面是巨大、持续变化、嵌套隐藏、元素匿名的**活状态**。本 skill 的全部职责是**在两者之间架桥**——让 agent 用最少的 token、最可靠的句柄，去**看清 → 指到 → 动手 → 看见结果**。

一切抽象从这条桥长出来；桥的形态可以整个重搭。四枝（感知 / 指认 / 操作 / 聚焦）是 agent 的四类意图，从根问题自然推出，但不是教条——若有更优的根结构，枝亦可动摇。

设计理念长成一棵树，新的抽象设计都是在树枝上分叉出新枝，一旦某个方向钻牛角尖堆屎山了，就把从根到叶的路径，从前向后审视，看每个抽象节点是否必要，是否是走了错误路线，是否长出了错误分支优化本不应该存在的问题。

许多人在读历史会产生一种错觉，觉得每个王朝的末期是一群坏人在干坏事才导致国家衰弱的。其实不是，大部分时候一个帝国的衰亡，是一群在这个系统里完全理性的好人，按照一套已经僵死的好规矩，合力把这艘船开进了冰山。亡国之君往往不是死于无法无天，而是死于作茧自缚。那些曾经维持了帝国稳定的法律和惯例，经过两百年的堆积，变成了一张密不透风的网。每一个既得利益者都趴在这张网上，合法地、合规地、甚至合乎道德地吸食帝国的血液。

Agent 的开发也是这样，一步加一个约束，然后相信前面的约束，继续加新的约束，不可避免的走向死亡螺旋。就成了屎山。必须每发展一定的程度，就重新做顶层抽象设计，进行激进的改革。项目当前的状态，就是一座屎山。

---

## 设计律（先于一切实现）

1. **从根到叶每个节点经得起"为什么存在"的拷问。** 一个抽象若只为修补另一个抽象的副作用而生——砍。

2. **一个问题一套机制。** 同一问题冒出第二个补丁时，停下从根到叶重审路径，而非叠第三个。

3. **设计规定"传达什么"，不规定"长什么样"。** 一切输出形态（标注符号、文件列数、命令语法糖、占位字形）都是可迭代的实现细节；本设计只锁定每个输出要承载的信息，不锁其字面形态。日后可整套换皮。

4. **零兼容。** 旧名 / 旧格式 / 旧语义一律删，不写迁移、不留 fallback。

---

## 顶层设计


- 浏览器连接
  - 检测用户可用浏览器
  - 检测用户默认浏览器
  - 设定数据路径 --user-data-dir，有默认值
  - list 时确保 cdp 浏览器已启动 --remote-debugging-port=9222 
- 浏览器任务
  - 感知
    - view 压缩页面为紧凑树，带结构、带信息
      - 用标识代表元素状态
        - 是否在视口内
        - 用 ref 指向 DOM，可直接操作 click fill hover focus
      - 对 Shadow 要穿透
    - 聚焦
      - 全页感知可依规则折叠指定区域
        - 聚焦规则持久化保存，用 uBlock Origin 的语法
      - 局部感知可指定区域
      - 站点聚焦 recipe (已知站点按规则输出重点信息)
        - 每文件可存多份规则，应对同一站点的不同路径，保存在 `rules/`
        - URL 作用域(hostname+pathname glob)匹配，`view`/`fetch` 默认启用、未命中回落树
        - 过程式规则，recipe 是 Node 模块，复用已有 API
  - 指认
    - ref 指向一个结构，有 DOM、父链
      - 可通过 ref 直接操作 DOM
      - 链式祖先记录
        - 可 ancestor 找祖先
        - 操作时发现 DOM 被毁，反馈存活的父元素内容
    - selector 跨页面、持久化匹配规则
  - 操作
    - 动作
      - 点击、填表
    - 反馈
      - 动作前后对比 DOM，将变化用 Tree 反馈给 agent
      - 动作前后对比 Tab 列表，新开页面让 agent 知晓
      - 动作后延迟1s，让页面充分反应，再做对比
- 浏览器日志
  - 新开 Tab，注入 Page.addScriptToEvaluateOnNewDocument
  - 用户新开 Tab，用daemon 监听注入
  
## 命令行

- 浏览器连接
  - `list` 列出所有 page tab,同时确保 CDP 浏览器已就绪
  - `open` 新开一个 tab
  - `close` 关闭一个 tab
  - `activate` 把指定 tab 拉到前台
- 脚本执行
  - `run` 跑一段自动化脚本(脚本内用全局 cdp API)
- 页面导航
  - `navigate` 让当前 tab 导航到指定 url
- 感知
  - `view` 感知页面：命中站点 recipe 输出聚焦摘要，否则压成带结构、带信息的紧凑树；`--tree`/建树意图强制树
  - `fetch <url>` 临时打开页面，用 `view`/`recipe` 感知抓取内容后，关闭页面，替代 web fetch MCP
- 自由执行
  - `eval` 在页面内执行任意 JS,回传 JSON 值
- 指认
  - `info <n> [--ancestor <k>]` 给目标元素的父链(tag/id/class/语义属性逐层),并附该 DOM 的 selector(已实现,接 `api.info`)
- 操作（目标均为 ref）
  - `click` 点击元素
  - `fill` 往输入框填值并触发 input/change
  - `focus` 聚焦元素
  - `hover` 鼠标悬停在元素上
  - `press-key` 按键 / 组合键(如 Enter、Ctrl+Shift+A)
  - `get-focus` 查看当前焦点落在哪个元素
- 观察
  - `screenshot` 截图
  - `logs` 读页面控制台日志

## 临时方案

fold / ignore-links 规则手动编辑 `rules/*.csv`(seed 自 `src/rules/`);recipe 手写 `rules/recipes/<site>.js`。